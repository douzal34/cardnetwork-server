export default {
  // Query: {
  //   partners: async (parent, {}, { models }) => {
  //     const partners = await models.Partner.findAll({
  //       order: [['createdAt', 'DESC']],
  //     });

  //     return partners;
  //   },
  //   partner: async (parent, { id }, { models }) => {
  //     return await models.Partner.findById(id);
  //   },
  // },

  // Mutation: {
  //   createPartner: async (parent, { name, description }, { models }) => {
  //     const partner = await models.Partner.create({
  //       text,
  //       description,
  //     });
  //     return partner;
  //   },
  // }
};